# WP-M22-003: Cross-Family Dependency Review

- Status: done
- Milestone: M22
- Owner: Codex

## Status transition

Selected by the user on 2026-08-05. This review selects only an M22-004
governance-promotion proposal; it does not select M22-004 or promote assets.

## Goal

Review the completed third family against the governed prismatic-hole and
rounded-slot evidence, then choose one narrowly scoped successor.

## Entry criteria

- The user explicitly selects this review workpack.
- Read the completed M22-002 review and update the coverage matrix with either
  a bounded knowledge unit, a counterexample, or an explicit no-reusable-
  knowledge outcome.

## Decision candidates

- propose a separate family-specific governance promotion;
- preregister an additive-boss followed by dependent-cut family; or
- if repeated cross-family evidence demonstrates that scripts cannot retain a
  correct prefix without the same structured dependency representation,
  propose the existing conditional IR shadow experiment.

## Boundaries

This review does not promote candidates, change runtime behavior, or authorize
external data, manifests, provider use, training, parser helpers, SDK, or IR.

## Result

Completed offline on 2026-08-05. The review records `multi-contour-pocket-v1`
as a third bounded `supported` knowledge unit and updates the coverage matrix
without closing generic topology, feature-recovery, or face/edge-dependency
gaps. It selects only `WP-M22-004` as a separately user-selected,
family-specific governance-promotion proposal. It rejects an immediate
additive-boss/dependent-cut design and retains the IR shadow as blocked for
lack of repeated direct prefix-failure evidence. No candidate lifecycle,
manifest, runtime, provider, parser, helper, SDK, or IR changed.
