# WP-M22-001: Multi-Contour Pocket Design and Preregistration

- Status: done
- Milestone: M22
- Owner: Codex

## Status transition

Selected by the user on 2026-08-05 after M22-000. This workpack authorizes
design and preregistration only; it does not select M22-002 or create an
asset, producer, executable manifest, or runtime route.

## Goal

Freeze a third self-authored sequence-paired family before producing any new
asset. The target capability is `Sketch(outer + inner loop) -> ExtrudeBase ->
CutPocket`; the deterministic oracle records the required base-sketch prefix.

## Scope

- Cite the target cells, known gaps, hypothesis, counterexample, and stopping
  rule from the approved modeling knowledge coverage matrix.
- Create a capability matrix showing the gap from `prismatic-hole-v1` and
  `rounded-slot-v1`: multi-contour sketch topology and an inner-profile cut.
- Preregister the exact development and held-out family-isolated rows,
  grammar boundary, deterministic-oracle provenance, canonical
  sequence/dependency contract, editability mutations, semantic
  anti-degeneration predicates, rejection taxonomy, and producer-stability
  checks.
- Create M22-002 as a separate controlled-production workpack.
- Record an evidence-seeking operation-contract draft, not a claimed
  kernel/API signature, for parameters, B-Rep delta, topology invariants, and
  unverified properties.

## Acceptance

- The selection and split are frozen before candidate production.
- A tracked preregistration record and M22-002 workpack make post-hoc case
  substitution, grammar expansion, or automatic admission impossible.
- `git diff --check` passes.

## Out of scope

Candidate production, promotion, executable manifests, external data,
provider/hosted use, training, runtime integration, parser expansion, helper,
SDK, or IR.

## Result

Completed offline on 2026-08-05. The preregistration freezes three centered
development rows and three offset held-out rows for `multi-contour-pocket-v1`,
its canonical four-operation dependency, parameter/editability contract,
semantic anti-degeneration controls, rejection taxonomy, and producer
stability requirement. The design cites the three target coverage cells and
records a non-API operation-contract draft. M22-002 is created as the sole
candidate-production route and requires separate user selection. No asset,
producer, manifest, provider, runtime, parser, helper, SDK, or IR changed.
