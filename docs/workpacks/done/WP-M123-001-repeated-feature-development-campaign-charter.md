# WP-M123-001: Repeated-Feature Development Campaign Charter

- Status: done
- Milestone: M123
- Owner: Codex
- Reviewer: not required (G1)
- Risk tier: G1

## Goal

Freeze one planning-only repeated-feature-pattern development campaign charter
so the first no-card five-family hosted candidate has an exact family-local
question, denominator and interpretation boundary without reopening hosted
scope or adding new mechanism evidence.

## Scope

- Draft one bounded single-family development campaign charter that names the
  exact three development rows, limited Q01 observation boundary, constrained
  four-cut action, interpretation table, and planned report/monitor paths.
- Keep the result explicitly below hosted-stability re-entry, preflight,
  authorization, and runtime-card scope; do not create a runnable G3 package.
- Update current status and planning navigation so `WP-TRG-020` is consumed and
  the remaining family-preparation queue is stated consistently.

## Compatibility constraints

Offline and credential-free. No held-out input access, provider construction,
preflight, authorization, request, runtime change, manifest change, family
evidence mutation, or runtime-card promotion.

## Acceptance

```powershell
python tools\check_governance.py
git diff --check
```

## Status transition

Update `status.md` first, then record the charter, planning-route documents,
completed workpack, and trigger archive. Close after governance audit and diff
checks pass.

## Owner acceptance

- Added the planning-only charter
  [`m123-repeated-feature-development-campaign-charter.md`](../../workflow/m123-repeated-feature-development-campaign-charter.md),
  which freezes the exact three development rows, no-card Q01 fact boundary,
  constrained rectangular-grid four-cut action, request accounting, report/
  monitor namespace, interpretation table, and explicit non-inference.
- Consumed `WP-TRG-020` into historical archive form and updated route/status
  records so repeated feature pattern is now a prepared family line rather than
  a still-pending trigger in the default queue.
- Kept the shared hosted-stability prerequisite unmet, so M123 stops before
  preflight and authorization. The charter is a frozen planning input for a
  later fresh family-scoped G3 readiness package, not a runnable campaign.

## Closure rationale

The repeated-feature family now has one single-valued no-card development
campaign boundary. Future hosted work no longer needs to rediscover its case
scope or interpretation layer; it only needs a fresh readiness/preflight
package after the shared hosted-stability route is reopened successfully.

## Out of scope

Shared hosted-stability re-entry, provider/model selection authority,
campaign preflight, user authorization, development execution, held-out
campaign design, or runtime guidance promotion.
