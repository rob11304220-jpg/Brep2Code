{
  "schema_version": 1,
  "unit_id": "prismatic-hole-v1",
  "unit_kind": "operation",
  "status": "reviewed",
  "question": "Which bounded self-authored sequence is evidenced for a rectangular base followed by one cylindrical cut?",
  "scope": {
    "kernel_operation": ["SketchRect on XY", "ExtrudeBase along +Z", "CutCylinder through, blind, or counterbore on the base"],
    "brep_observables": ["single prismatic base solid", "cylindrical cut faces", "planar base faces", "cut axis parallel to +Z in the deterministic oracle"],
    "preconditions": ["one frozen prismatic-hole-v1 record", "self-authored deterministic reference", "recorded split and canonical operation parameters"],
    "unsupported_conditions": ["B-Rep-to-sequence inference", "native-history recovery", "face- or edge-referenced downstream features", "automatic case or manifest admission"]
  },
  "operation_contract": {
    "function_family": ["SketchRect", "ExtrudeBase", "CutCylinder"],
    "parameter_contract": ["Only the frozen record-specific base, centre, radius, depth, bore-radius, and bore-depth parameters in prismatic-hole-v1 are evidenced."],
    "expected_brep_delta": ["A single prismatic base receives one cylindrical through, blind, or counterbore subtraction under the frozen oracle."],
    "topology_invariants": ["The exact canonical sequence and recorded cut variant must agree with the deterministic reference and replay audit."],
    "numeric_or_tolerance_boundary": ["No general parameter domain or numeric tolerance is claimed; values are those in the frozen expansion record."],
    "unverified_properties": ["Generic cylindrical-feature recognition, alternate sequences, and arbitrary target/axis placement."]
  },
  "sequence": {
    "canonical_pattern": ["SketchRect", "ExtrudeBase", "CutCylinder"],
    "dependencies": ["ExtrudeBase consumes SketchRect", "CutCylinder targets the produced base", "cut parameters are family-record specific"],
    "admissible_alternatives": []
  },
  "evidence": {
    "level": "supported",
    "source_kind": "self_authored_deterministic_oracle",
    "supporting_case_ids": ["param_through_hole_low", "param_through_hole_nominal", "param_through_hole_high", "param_counterbore_low", "param_counterbore_nominal", "param_counterbore_high"],
    "held_out_case_ids": ["param_blind_hole_low", "param_blind_hole_nominal", "param_blind_hole_high"],
    "audit_links": ["docs/corpus/sequence-paired/prismatic-hole-v1-expansion.json", "docs/architecture/v1/m20-prismatic-hole-governance-promotion-review.md"],
    "authority_links": ["docs/architecture/adr/0019-prismatic-hole-sequence-pair-governance.md"]
  },
  "failure_and_repair": {
    "observable_signals": ["frozen-sequence mismatch", "split leakage", "hash/path drift", "failed deterministic replay or editability mutation"],
    "safe_action": "Reject the record from this family-scoped evidence set and preserve the audit finding; do not substitute a different sequence or infer a generic repair.",
    "stop_conditions": ["any grammar expansion", "any case substitution or split move", "evidence request outside the frozen single-cut family"],
    "counterexamples": ["This family does not support multi-contour pockets, profile orientation resolution, or generic history reconstruction."]
  },
  "runtime_projection": {
    "eligible": false,
    "experience_card_id": null,
    "review_trigger": "No runtime projection is justified: the evidence is a family-governance and deterministic-oracle contract, not three independent direct runtime mechanisms."
  }
}
