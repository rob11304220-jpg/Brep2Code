# M29 Selector-Ambiguity Controlled Production Review

## Result

Both preregistered experimental twin-boss candidates were produced twice with
stable normalized STEP hashes. Their unchanged planar +Z maximum-output-Z
predicate found exactly two faces, recorded `ambiguous`, and ended before any
selected-face-dependent sketch or cut. Existing unique-selector records remain
the oracle; neither candidate is registered, manifested, nor runtime material.

## Evidence

| Role | Evidence | Result |
|---|---|---|
| oracle | existing `face-selected-dependent-cut-v1` | cardinality one remains the only permitted bind |
| discriminating | centered and offset twin-boss candidates | cardinality two, fail-closed |
| negative control | wrong-face injection | rejected as `selector_ambiguity` |
| negative control | coordinate-only injection | rejected as `coordinate_only_support` |

Focused tests passed (3), generic intake audit passed, family audit passed for
two rows, and Ruff passed for the producer, auditor, and focused tests.

## Disposition

The result supports the bounded Q01 observable unit
`planar-face-selector-cardinality-v1`. It does not support generic selector
recovery, persistent naming, a runtime selector/helper, active-library
promotion, manifest use, or any hosted evaluation.
